현재 원소와 바로 다음 원소를 비교해서 다르다면
다른 배열에 그 원소를 넣으면 되지 않을까?
<< 라는 생각을 해서 코드가 짧게 끝난 듯 하다.

but 다음 원소를 비교하게 되면 마지막 원소는 비교할게 없기 때문에 쓰레기 값이 들어가게 된다.

그래서 미리 0번째 원소 값을 넣어놓고
for문을 1번째부터 돌리는 천재적인 생각을 어디선가 퍼왔다.

또한 배열에 다른 배열 값을 넣는 함수를 몰라서
answer(arr[i])라는 무식한 방법을 썼었다.

정답은 answer.push_back(arr[i])였다.
