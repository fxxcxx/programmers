문자열을 비교하라해서 되게 어렵게 생각했는데
알아서 아스키코드 값으로 비교가 되더라.
또 대문자는 아스키코드가 소문자보다 작다고 한다. 

이것만 알면 temp 를 만들어 비교하면 된다.
temp(여기선 r)은 char로 선언해도 되고, int로 선언해도 된다.
어차피 아스키코드로 비교하는건 같으니까~

근데 string은 안된다 << 문자열이 아니기 땜문(내 예상)
